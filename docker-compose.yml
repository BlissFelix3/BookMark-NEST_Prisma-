version: 'version'
services:
  dev-db:
    image: postgres:13
    ports:
      - 5434:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: abhademere18
      POSTGRES_DB: bookmark
    networks:
      - my-network

networks:
  my-network:
